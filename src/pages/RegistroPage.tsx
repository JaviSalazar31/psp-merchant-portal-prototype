import PreloginLayout from '@/components/prelogin/PreloginLayout';
import RegistroForm from '@/components/prelogin/RegistroForm';

export default function RegistroPage() {
  return (
    <PreloginLayout>
      <RegistroForm />
    </PreloginLayout>
  );
}
